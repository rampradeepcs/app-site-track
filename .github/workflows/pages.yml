name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths: ["src/**", "public/**", "next.config.ts", "package.json", ".github/workflows/pages.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Never let two Pages deploys race; the newest push wins.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci

      # Pages must already be enabled with Source = "GitHub Actions"
      # (Settings -> Pages). `enablement: true` cannot do it: the workflow
      # token may deploy to Pages but not create the site, and asking it to
      # fails with "Resource not accessible by integration".
      - uses: actions/configure-pages@v5

      - name: Build static export
        run: npm run build
        env:
          GITHUB_PAGES: "true"

      # Pages serves the artifact as-is; .nojekyll stops it stripping the
      # _next/ directory, which would break every asset.
      - run: touch out/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
