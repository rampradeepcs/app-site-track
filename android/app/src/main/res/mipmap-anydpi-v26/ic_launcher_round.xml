<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background"/>
    <foreground android:drawable="@drawable/ic_launcher_foreground_wf"/>
    <!-- A silhouette already, so it doubles as the Android 13+ themed-icon
         layer; the system reads its alpha and tints it. -->
    <monochrome android:drawable="@drawable/ic_launcher_foreground_wf"/>
</adaptive-icon>
